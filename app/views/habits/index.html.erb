<h1>DO DAILY.</h1>
<p>You already know what to do.</p>

<p><%= link_to "New Habit", new_habit_path %></p>

<ul>
  <% @habits.each do |habit| %>
    <li>
      <strong><%= habit.title %></strong>
      <% if habit.description.present? %>
        <br>
        <small><%= habit.description %></small>
      <% end %>

      |
      <%= link_to "Edit", edit_habit_path(habit) %>
      |
      <%= link_to "Delete", habit_path(habit),
                  data: { turbo_method: :delete, turbo_confirm: "Delete this habit?" } %>
    </li>
  <% end %>
</ul>